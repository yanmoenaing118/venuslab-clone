import styles from "./../../styles/Section.module.scss";

export default function Section({ img_first }) {
  return (
    <section className={styles.section}>
      <article></article>
      <article>right</article>
    </section>
  );
}
